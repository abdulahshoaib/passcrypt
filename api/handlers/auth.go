package handlers

import ()

// Register to the service
func RegisterHandler() {
	// This should ask user for his username
	// a passcode for that username

	// server creates a db entry into against username
	// db also keeps a hashed version of the passcode
	// The passcode will be the valut name
}

// should delete the user data if sent with correct passcode
func DeleteHandler()  {
	// Delete query onto the database to delte the user's data
}
